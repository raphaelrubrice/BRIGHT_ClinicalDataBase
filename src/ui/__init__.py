"""Module to define a simple UI"""
# hf_cache/ here is an identical copy of the EDS-pseudo model cache in src/database/hf_cache/.
# The duplicate exists so the GUI loads edsnlp via its own local path without depending on
# the database module's sys.path modifications.