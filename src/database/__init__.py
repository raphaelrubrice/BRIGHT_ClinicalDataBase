"""Module to handle the databases"""
# hf_cache/ here and in src/ui/hf_cache/ are identical copies of the EDS-pseudo model.
# They are kept separate so the GUI (app_qt.py) can load edsnlp independently of the
# database pipeline without cross-module path conflicts.